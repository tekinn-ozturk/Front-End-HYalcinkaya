let firstName = "hAKan";
let lastName = "YalCINKaya";
let training =`Template Literals JS`;

let info = `${firstName} ${lastName}`;
console.log(info);

firstName= `${firstName[0].toUpperCase()}${firstName.toLowerCase().slice(1,100)}`;
console.log(firstName);

